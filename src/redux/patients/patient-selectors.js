export const patientListSelector = (state) => state.patients.patientList;
export const patientIdSelector = (state) => state.patients.id;
export const patientInfoSelector = (state) => state.patients.patient;
export const filterSelector = (state) => state.patients.filter;
